import point from 'app/entities/point/point.reducer';
import datasource from 'app/entities/datasource/datasource.reducer';

const entitiesReducers = {
  point,
  datasource
};

export default entitiesReducers;
